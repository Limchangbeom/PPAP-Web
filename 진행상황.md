# PPAP-Web 진행상황 및 남은 작업 목록

작성일: 2026-09-13 / 다음 마일스톤: 17일 이전

## 1. 완료된 작업

### 1.1 멘토 피드백 반영
- 응답 간 논리 충돌(consistency) 항목: 해당 문항 하이라이트 + 충돌 문항 페이지로 이동 + 문항별 메시지 표시
- 미응답 문항: 해당 문항 하이라이트 + 미완료 페이지로 이동
- 페이지당 문항 수 동적 조정: 남은 문항이 1~2개면 10개 제한을 풀고 이전 페이지에 합쳐서 표시
- 책임 소재 명시: 메인 페이지에 사용자 책임 면책 고지 추가 (이후 박스형·단락 분리로 축소 정리)

### 1.2 데이터 분리 (`diagnostic_content.py` 하드코딩 해소)
- `data/questions.py`: 진단항목 109개 분리 (`QUESTIONS`, `QUESTION_INDEX`, `PAGE_SIZE`)
- `data/guidance.py`: 가이드 76건 분리 (`LEGAL_BASIS_GUIDANCE` 58건, `TERM_GUIDANCE` 18건)
- `diagnostic_content.py`는 로직 함수만 유지, `data/` 패키지에서 import
- 진단 문항·선택지·평가 기준은 변경 없음 (별칭·매칭 로직 포함)

### 1.3 가이드 제공 형태 변경 (인라인 → 모달/팝업)
- 문항별 1~2줄 인라인 설명 제거 → "가이드 보기" 버튼 + 모달 팝업
- 팝업 형식 통일 (위→아래 순서): 진단항목의 지문 → 관련법령(원문) → 관련법령(해석) → 진단항목의 근거 → 용어
- GitHub 원문(`GDPR.md` / `CCPA.md`) 기반으로 가이드 해설 76건 보강 (조문 번호·원문 취지·판례 연결)
- CSP(`script-src 'self'`)와 인라인 스크립트 충돌 해결: 실행 코드는 외부 `app.js`, 데이터는 JSON 블록으로 분리

### 1.4 취약점 진단 및 패치 (상세: `취약점패치.md`)
- CSRF: 토큰 검증 + `/reset` GET→POST 변경
- XSS: `tojson` + `textContent` 방식 적용
- 보안 헤더(CSP·HSTS·X-Frame-Options 등), 세션 쿠키(Secure·HttpOnly·SameSite) 적용
- 가이드 모달 미동작·헤더 브랜드 미출력 등 기능 버그 수정

### 1.5 결과 리포트 가독성 개선
- 카드 나열식 → 서술형(글형태) 보고서로 변경 (`result.html`, `print_report.html`)
- 4개 상태 카드(위반·미흡·충족·권장 건수) 히어로 영역에 복원 + 상태별 의미 설명 추가
- 판단 근거 / 권고 조치 / 관련 조문을 각자 다른 줄로 분리
- 항목 내부는 좁게(2px)·항목 사이는 넓게(20px) 조정, 박스 밖 텍스트 넘침 수정
- GDPR 역할 표기 정리: 선택지 `controller (개인정보처리자)` / `processor (처리수탁자)` 병기

## 2. 진행 중 (17일 이전 목표)

1. **개정법률 안내 방안 검토**
   - 국가법령정보센터 조문별 연혁·대비표 참고, 관련 API 존재 유무 확인 후 대응방안 적용
2. **AI 자동진단 방향성 검토**
   - 회사 개인정보처리방침을 AI가 진단항목과 비교하는 방식의 실현 가능성·구현 방안 마련
   - 적용이 어렵더라도 추후 방향성 문서 수준으로 정리

## 3. 논의 후 진행 (미확정)

| 항목 | 상태 |
|---|---|
| 로그인/회원가입 + 과거 기록 비교분석 (관리자/사용자 구분) | 논의 필요, 구현 자체는 가능 |
| 문의 기능 (사용자 작성·관리자 처리, IP/계정당 rate limit) | 로그인 기능과 함께 논의 |
| 과징금 유추 서비스 (EU 규정·미국 판례 기반) | 추가 리서치 완료 후, 제공 방식(문항별/리포트별) 의견 수렴 |
| 육각형 그래프 강점·약점 출력 | 매핑테이블 완성 후, 항목 분류·가중치 기준 필요 |
| 변경된 진단항목 적용 (Q8·Q9·Q11·Q12·Q15 중복 해소, 제3자 제공 없음 스킵 여부 등) | 리서치 결과 대기 |
| 가이드 통일 | 의견 수렴 후 진행 |
| 시행령 추가 리서치 | 시간상 향후 방향성 제시용으로만 유지 |

## 4. 리서치 팀 요청 사항

- Q8·Q9·Q11·Q12·Q15이 중복 질문처럼 보이는 문제 해소 방안
- 제3자 제공이 없는 경우 해당 페이지 스킵 가능 여부 (다른 스킵 가능 항목 포함, 변경사항 정리)
- 과징금 제공 방식 의견 (문항별 하단 표시 vs 리포트 항목 추가 등)
- 육각형 그래프용 항목 분류·가중치 기준

## 5. 참고

- 시행령·과징금 상세 리서치는 시간상 향후 방향성 제시용이며 현 마일스톤 범위 밖.
- 실제 기업 처리방침 검증(AWS 서버 자체 검증)은 웹 적용 가능성 검토 목적이며,
  정식 "검증결과"와는 별개로 진행.
- 리서치 이후 변경·수정 요청은 수시로 받아서 반영.
