# 취약점 점검 및 패치 내역

대상: PPAP-Web (Flask 기반 개인정보 처리방침 진단 도구)
작성일: 2026-09-13

> 진단 문항(QUESTIONS, 109개) 및 평가 로직은 변경하지 않았습니다.
> 본 문서는 보안 취약점과 기능 버그 패치 내역만 정리합니다.

## 1. CSRF (Cross-Site Request Forgery)

### 발견 내용
- 상태 변경 요청(`POST /start`, `POST /diagnosis`, `/reset`)에 CSRF 토큰 검증이 없거나,
  `/reset`이 `GET` 방식으로 동작하여 악성 링크 클릭만으로 진단 상태가 초기화될 수 있었음.
- 폼에 CSRF 토큰 필드가 없었음.

### 패치 내용 (`app.py`, `templates/base.html`, `templates/diagnosis.html`, `templates/index.html`, `templates/result.html`)
- `generate_csrf_token()` / `csrf_protect()` (`before_request`) 추가.
  `POST/PUT/DELETE/PATCH` 요청은 세션 토큰과 폼 토큰을 `hmac.compare_digest`로 비교하며,
  불일치 시 `403`으로 차단.
- `base.html`에 `csrf_input()` 매크로 추가 후 모든 상태 변경 폼에 적용.
- `/reset` 라우트를 `GET` → `POST`로 변경하고, "처음으로 돌아가기" 링크를
  CSRF 토큰 포함 POST 폼으로 교체.

### 검증
- 토큰 없이 `POST /start` → `403`
- `GET /reset` → `405`, 토큰 없이 `POST /reset` → `403`, 토큰 포함 시 → `302` 정상 동작 확인.

## 2. XSS (Cross-Site Scripting)

### 발견 내용
- `diagnosis.html` 모달용 인라인 스크립트에서 `legalBasis: "{{ question.legal_basis }}"`,
  `desc: "{{ question.description }}"` 형태로 직접 문자열 보간 → 따옴표·스크립트 삽입 가능.
- 모달 렌더링 일부에 `innerHTML` 사용 가능 구조.

### 패치 내용 (`templates/diagnosis.html`, `static/js/app.js`)
- 모든 서버→JS 데이터 전달에 `| tojson` 필터 적용 (JSON 이스케이프 보장).
- 모달 DOM 생성은 `textContent` / `createTextNode`만 사용 (`innerHTML`로 사용자·DB 데이터 삽입 금지).
- 가이드 데이터는 `<script type="application/json" id="guidance-data">` 데이터 블록으로 전달하고,
  실제 모달 동작 코드는 외부 파일 `static/js/app.js`로 이동.

## 3. CSP와 인라인 스크립트 충돌 (가이드 모달이 열리지 않던 원인)

### 발견 내용
- 보안 헤더로 `Content-Security-Policy: script-src 'self'`를 적용한 뒤,
  `diagnosis.html`의 인라인 `<script>`와 `print_report.html`의 인라인 `window.print()`가
  브라우저에서 차단되어 가이드 모달이 전혀 열리지 않았음.

### 패치 내용
- `diagnosis.html`: 인라인 실행 스크립트 제거 → JSON 데이터 블록만 유지.
- `static/js/app.js`: `initGuidanceModal()` (열기/닫기/배경 클릭/Esc) 추가.
  기존 인라인 코드에 있던 `h2` 미정의 버그도 함께 수정.
- `print_report.html`: 인라인 스크립트 제거 → `app.js` 로드 추가 후
  `report-page` 클래스 감지로 자동 인쇄 (`window.print()`)하도록 이동.
- CSP는 `script-src 'self'` 그대로 유지 (인라인 허용 없이 보안 수준 유지).

## 4. 보안 헤더 미적용

### 발견 내용
- `X-Content-Type-Options`, `X-Frame-Options`, `HSTS`, `CSP`,
  `Referrer-Policy`, `Permissions-Policy` 미설정.

### 패치 내용 (`app.py` → `set_security_headers`, `after_request`)
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY` (클릭재킹 방지)
- `X-XSS-Protection: 1; mode=block`
- `Strict-Transport-Security: max-age=31536000; includeSubDomains`
- `Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data:; font-src 'self' https://fonts.gstatic.com; connect-src 'self'`
  (Google Fonts 도메인 허용 — 미허용 시 `IBM Plex Sans KR` 로딩 차단으로 원래 웹 서식이 깨져 보임)
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: geolocation=(), microphone=(), camera=()`

## 5. 세션 쿠키 보안 속성 미흡

### 발견 내용
- 세션 쿠키에 `Secure` / `HttpOnly` / `SameSite` 미설정.

### 패치 내용 (`app.py`)
- `SESSION_COOKIE_SECURE = True`
- `SESSION_COOKIE_HTTPONLY = True`
- `SESSION_COOKIE_SAMESITE = "Lax"`
- `PERMANENT_SESSION_LIFETIME = 3600` (세션 수명 제한)
- `SECRET_KEY`는 환경변수(`FLASK_SECRET_KEY`) 우선, 없으면 실행 시마다 무작위 생성.
- 요청 본문 상한 `MAX_CONTENT_LENGTH = 1MB` 설정.

## 6. 기타 기능 버그 수정 (보안 외)

| # | 내용 | 파일 |
|---|------|------|
| 1 | 모달 JS `h2` 변수 미정의로 스크립트 전체 중단 | `templates/diagnosis.html` → `static/js/app.js`로 이동하며 수정 |
| 2 | `TERM_GUIDANCE = TERM_GUIDANCE = [` 중복 할당 | `data/guidance.py` |
| 3 | 헤더 브랜드(`PPAP` / 풀네임) 미출력 — `project_name`이 템플릿 컨텍스트에 없어 빈 파란 알약으로 표시 | `app.py`에 Jinja2 globals 등록으로 수정 |
| 4 | GDPR 역할 UI 라벨 `개인정보처리자/처리수탁자` → `controller/processor` 통일 (표시 라벨만 변경, 진단 문항·법령 원문은 무변경) | `templates/index.html`, `diagnostic_content.py` (`role_label`, `_role_label`), `site_content.py`, `app.py` 플래시 문구 |
| 5 | 메인 페이지 면책(주의사항) 섹션 과다한 세로 크기 → 한 박스·작은 글씨로 축소 | `templates/index.html` (`disclaimer-compact`), `static/css/styles.css` |
| 6 | 결과·리포트 화면 카드 나열식 → 서술형(글형태) 보고서로 변경 | `templates/result.html`, `templates/print_report.html`, `static/css/styles.css` (`narrative-*`) |

## 7. 진단항목 무변경 확인

- `data/questions.py`: 수정 없음. `QUESTIONS` 109개, `PAGE_SIZE = 10` 유지.
  첫 문항 `gdpr_scope_detail_person`, 마지막 문항 `ccpa_cookie_controls`, ID 중복 없음 확인.
- `data/guidance.py`: 진단 판정에 쓰이는 가이드 문구 자체는 변경 없음.
  수정은 중복 할당 구문 1건のみ.
- 역할 명칭 변경은 화면 표시용 라벨(`role_label`, `_role_label`, 템플릿, 안내 문구)에 한정되며,
  문항 텍스트·선택지·평가 기준(`evaluation`)은 손대지 않았음.

## 8. 실행 방법

```bash
cd C:\Users\s2205\OneDrive\Desktop\PPAP-Web
pip install Flask==3.0.3
python app.py
```

브라우저에서 `http://localhost:5000` 접속.

## 9. 가이드 신뢰성 보강 (2026-09-13 추가)

- `data/guidance.py`의 해설 문구 76건(법령 58 + 용어 18)을
  `https://github.com/Limchangbeom/PPAP`의 `GDPR.md` / `CCPA.md` 검토 내용 기준으로 보강.
- 반영 예시: 제6조 6개 적법근거 열거, Chapter V 3단계(적정성→보호조치→예외) 구조,
  제8조 16세·13~16세 조정 범위, 제22조 예외 3가지+보호장치, §1798.120 미성년 opt-in의
  판매·공유 한정, CCPA에 국외이전 전용 동의 규정 없음 명시, Sephora 사례 연결 등.
- 별칭(매칭 키)·문항·선택지·평가 기준은 무변경. 해설 문구만 교체.
- 면책·리포트 텍스트는 단락 분리 + `overflow-wrap` + 행간 조정으로 깨짐/튀어나옴 완화.

## 10. 남은 권장 사항 (미적용)

- `style-src 'unsafe-inline'` 제거 후 인라인 `style` 속성을 CSS 클래스로 이전.
- 로그인·권한 체계 도입 시 세션 고정 공격 대비 세션 ID 재발급.
- 운영 배포 시 `Flask` 내장 서버 대신 WSGI 서버(gunicorn/waitress) + HTTPS 리버스 프록시 사용.
- `SECRET_KEY`를 환경변수로 고정 (현재 미설정 시 재시작마다 변경되어 기존 세션 무효화됨).
