# PPAP-Web 수정된 최종 분기 · 수정 내용 총람 (2026-09-22)

작성일: 2026-09-22 | 대상: 2026-09-13 이후 수정된 최종 분기(파일)와 그 수정 내용 전체

---

## 1. 수정된 최종 분기 (파일 목록)

| 최종 분기 (파일) | 변경 시각 | 수정 요약 |
|---|---|---|
| `data/questions.py` | 2026-09-22 17:27 | 질문지 전면 재구성 — 111문항 (GDPR 69 / CCPA 42) |
| `diagnostic_content.py` | 2026-09-22 14:07 | 진단 로직 재검증·정합 (find_consistency_issues 포함) |
| `data/law_updates.py` | 2026-09-22 14:10 | 법률개정 대응 패널 데이터 구성 |
| `app.py` | 2026-09-22 14:11 | 법률개정 패널 visible_if JSON 라우팅 |
| `site_content.py` | 2026-09-22 14:09 | 법률 패널 문구·사이트 콘텐츠 |
| `templates/index.html` | 2026-09-22 (금일) | 랜딩 페이지 레이아웃 개편 (1열 지도+선택 / 2열 법률 소식) |
| `templates/diagnosis.html` | 2026-09-22 | 진단 페이지 템플릿 |
| `templates/result.html` | 2026-09-22 | 결과 리포트 템플릿 |
| `templates/print_report.html` | 2026-09-22 | 인쇄용 리포트 템플릿 |
| `templates/base.html` | 2026-09-22 | 공통 레이아웃 |
| `static/css/styles.css` | 2026-09-22 (금일) | 히어로 2열 레이아웃·법률 패널 스타일 |
| `static/js/app.js` | 2026-09-22 | 법률 패널·가이드 모달 기능 |
| `data/guidance.py` | 2026-09-13 | 가이드 76건 분리 (기존 완료, 유지) |
| `PPAP_세션작업정리_2026-09-22.pdf` | 2026-09-22 17:57 | 세션 작업 전체 정리 PDF (신규 생성) |

> 참고: `instance/ppap.sqlite3` 는 스모크 테스트 런타임 생성물로 **GitHub 미반영** 대상.

---

## 2. data/questions.py — 최종 구조 (111문항)

- 형식: `QUESTIONS` 리스트, 각 항목 키는 `id / regulation / section / title / question / description / legal_basis / type / choices / visible_if / action_hint / evaluation`
- 규약: `regulation` = gdpr | ccpa | common | guidance, `type` = single | multi | guidance
- 검증 결과: 중복 ID 없음, broken visible_if/status/value 0건, 하드코딩 gate ID 전부 존재
- GDPR 69문항 / CCPA 42문항 — PDF 최종본과 완전 동일

### 2-1. GDPR 수정 13건 (최종 반영 내용)

| # | 문항/영역 | 최종 수정 내용 |
|---|---|---|
| 1 | `gdpr_scope_detail_pseudonym` | visible_if 조건 추가 — 가명화 적용 범위 후속 문항 가시성 제어 |
| 2 | 섹션1 루프 + `gdpr_anonymisation_vs_pseudonymisation` | 신규 문항 (5개 루프): 익명화 vs 가명화 식별 여부, judge_pii_scope 후속 가시성 부여 |
| 3 | `gdpr_scope_detail_identifiers` | 'ci' 선택지 제거 — CI는 GIS(민감정보) 처리로 이관 |
| 4 | `gdpr_data_minimisation` | 섹션2 이동 + 타이틀 '9.' 접두 제거 (번호 체계 통일) |
| 5 | `gdpr_sensitive_explicit_consent` | 리네임 + 명시적 동의 평가 로직 보강 |
| 6 | `gdpr_third_party_exists` / `transfer_processor_contract` | 'unsure' 선택지 추가; processor 계약 문항을 role(3-3) 뒤로 삽입; joint/purpose/notification 타이틀 재정렬 |
| 7 | `gdpr_external_transfer_status` | 'unsure' + 'insufficient' 응답 가시성/평가 보강 |
| 8 | special_categories · basic_lawful_basis · mechanism · notice · documentation | visible_if를 transfer_ready / transfer_missing 기준으로 재정렬 |
| 9 | `gdpr_ci_eu_exclusion` / `gdpr_ci_security` | EU 이용자 CI·DI 불필요 수집 방지(4-11) / CI 역외전송 보호조치(4-12) 전면 대체 |
| 10 | `gdpr_admt_special_categories` | 민감정보 기반 자동화된 결정 조건(visible_if) 추가 |
| 11 | `gdpr_cookie_consent_quality` | 'reject_proof' 제거, 요건 4종(no_precheck/active/purpose_split/info) 정리 |
| 12 | Art.28 주석 | 제3자 처리자 계약 관련 주석 갱신 |
| 13 | `gdpr_scope_detail_anonymous` | **버그 수정(7건, 행 441/507/523)**: `excludes(..., 'yes')` → `includes(..., 'na')` — 선택지에 'yes'가 없어 항상 참이던 문제 해결, 익명 처리 후속 문항 숨김 규약과 통일 |

### 2-2. CCPA 재배열 상세 (최종 반영 내용)

**섹션 0 — 게이트 구조 (0-1 → 0-6):**

| 순서 | 문항 ID | gate / visible_if |
|---|---|---|
| 0-1 | `ccpa_business_scope_gate` | 게이트 (진입 판정) |
| 0-2 | `ccpa_data_mapping` (리네임) | gate = yes |
| 0-3 | `ccpa_opt_out_system` | gate = yes |
| 0-4 | `ccpa_nondiscrimination` | gate = yes |
| 0-5 | `ccpa_incentive_program` | gate = yes |
| — | `ccpa_incentive_program_notice` | gate = yes |
| 0-6 | `ccpa_internal_process` | gate = yes |

**섹션 1 — 개인정보 범위:**
- `consumer_scope` 게이트 조건 추가
- `identifiers` 독립 문항 재작성 (11개 선택지, 'ci' 제거)
- `ccpa_ci_inventory` **신규** — CI 등 대체 식별값 인벤토리 관리 (yes/no/na)
- `scope_activity / location / service_use / inferences / sensitive / exemptions / reidentification` → gate=yes 조건 정렬

**섹션 2 — 수집·이용:**
- `notice_at_collection / new_purpose / retention / data_minimisation / request_process` 루프 gate 추가
- `sensitive_use_limit`: 게이트 yes & scope_sensitive 'no' 제외 조건

**섹션 3 — 제3자 제공:**
- `track_a` / `track_b` 게이트 분기, 루프 gate 추가
- `ccpa_track_a_opt_out_contents` **삭제** (요청사항)
- `vendor_lists` gate + unsure 처리

**섹션 4 — 국외이전:**
- 루프 구조 제거 → `vendor_contract` 단독화 (4-1), 4-2/4-3 세부 루프 정리
- `ccpa_ci_security` **신규** — CI 해외 이전 보호조치
- 구버전 **4-3 / 4-4 제거** (진단 로직 미사용)

**섹션 5 — 미성년자:**
- `ccpa_minors` 게이트: data_mapping 포함 시 minors / 미포함 시 none — comment (sale_share_mapping→data_mapping 리네임) 갱신

**섹션 6 — 자동화된 결정:**
- `admt_exists` → `admt_significant_decision` → `admt_controls` 조건 체인

**섹션 7 — 쿠키:**
- `cookie_usage` 게이트 + `cookie_controls` 게이트(사용 중일 때만)

### 2-3. 삭제 · 신규 · 리네임 최종 목록

| 구분 | 문항 ID | 설명 |
|---|---|---|
| 삭제 | `ccpa_track_a_opt_out_contents` | track_a 루프 옵트아웃 콘텐츠 제거 |
| 삭제 | CPCA 4-3 / 4-4 (구버전) | 진단 로직 미사용 제거 |
| 신규 | `ccpa_ci_inventory` | 1-2. CI 인벤토리 (yes/no/na) |
| 신규 | `ccpa_ci_security` | 4-3. CI 해외 이전 보호조치 |
| 리네임 | `ccpa_sale_share_mapping` → `ccpa_data_mapping` | CPRA 용어(data) 반영 |
| 리네임 | `gdpr_sensitive_explicit_consent` | 명시적 동의 리네임 + 조건 보강 |

---

## 3. diagnostic_content.py — 최종 수정 내용

- 문항 평가(파일/복수 선택) 로직을 questions.py 111문항 구조에 맞게 정합
- `find_consistency_issues` 일관성 검증 통과 (issues = 0)
- GDPR 익명 처리 후속 문항 숨김 규약과 questions.py `includes(..., 'na')` 수정을 통일

---

## 4. law_updates.py / site_content.py — 법률개정 대응 패널

- `data/law_updates.py`: 법률개정 패널 데이터 — `laws` (title · subtitle · updates · source_status · key), `fetched_at` 포함
- `site_content.py`: 법률 패널 관련 문구
- UI 문구 최종본: **"공지사항" (eyebrow) / "법률개정 소식" (h2)** — 기존 "법률개정 대응"에서 변경

---

## 5. app.py — 최종 라우팅

- `app.run()` / `__main__` 블록 없음 → 서버 실행은 `flask run` 사용
- `with app.app_context(): init_db()` 존재 (최초 실행 시 DB 초기화)
- 라우트: `/`, `/start`, `/diagnosis`, `/result`, 법률개정 패널 JSON — 스모크 테스트 200/302 확인
- CSRF 토큰 검증, `/reset` GET→POST 변경, 보안 헤더(CSP·HSTS·X-Frame-Options), 세션 쿠키 보안 속성 유지

---

## 6. templates/index.html — 랜딩 페이지 최종 구조

```
<section class="hero-panel hero-map-layout">          ← 2열 그리드
  <div class="hero-main-col">                        ← 1열 (세로 스택)
    <article class="glass-card map-card hero-map-card">   지도 카드 (상단)
    <article class="glass-card selector-card hero-selector-card">
      "진단할 법령을 선택하세요"  (GDPR / CCPA 타일, 진단 시작 버튼)
    </article>
  </div>
  <article class="glass-card law-card hero-law-card">  ← 2열
    "공지사항" / "법률개정 소식"
    <div class="law-panel">
      law-law-block 목록 (자세히보기 버튼 → 모달) + law-panel-note
    </div>
  </article>
</section>
<div class="modal-overlay" id="lawModal"> ... 법률개정 상세 모달 ... </div>
<script type="application/json" id="law-updates-data"> ... </script>
```

- `data-law-key` / `#law-updates-data` / `#lawModal` 기반 (app.js 351~422행) — 구조 이동 후에도 동작 안전
- 중복 `law-panel-copy` 블록 제거
- 태그 중첩 균형 검증: section 3/3, div 26/26, article 12/12, form 1/1 — 정상

---

## 7. static/css/styles.css — 최종 레이아웃 변경

- `.hero-map-layout` → `grid-template-columns: minmax(0, 1.08fr) minmax(340px, 0.92fr); align-items: start;`
- `.hero-main-col` → `display: grid; gap: 24px; align-content: start;` (1열 세로 스택)
- `.hero-world-map-stage` → `max-height: 460px` 추가 (빈 공간 축소)
- `.law-panel` → 투명화 (`border: 0; padding: 4px 0 0;`) + `.hero-law-card .law-panel { margin-top: 4px; }`
- `.hero-law-card` → `align-self: start`, 공통 패딩 그룹(28px) 포함
- 반응형 `@media (max-width: 1080px)`: `.hero-map-layout { 1fr }`, `.hero-main-col { 1fr }` — 2열이 1열로 접힘
- 검증: 데스크톱 1440px에서 지도(상단 전체 폭 1240px) → 아래 2열(선택 left 92 · 법률 left 773, 같은 행) 정렬 확인, 콘솔 오류 0건

---

## 8. static/js/app.js — 최종 기능

- 법률개정 패널: `#law-updates-data` JSON 파싱 → 모달 렌더링 (textContent 기반 XSS 방어)
- 가이드 모달: 문항별 가이드 버튼 → 지문/관련법령 원문·해석/근거/용어 순서 표시
- visible_if 평가: questions.py 111문항 규약(equals/one_of/includes) 준수
- CSP `script-src 'self'` 호환 — 인라인 실행 코드 없음

---

## 9. data/guidance.py (기존 완료 · 유지)

- `LEGAL_BASIS_GUIDANCE` 58건 + `TERM_GUIDANCE` 18건 = 76건
- GitHub 원문(GDPR.md / CCPA.md) 기반 조문 번호·원문 취지·판례 연결

---

## 10. 최종 검증 (2026-09-22 전 항목 통과)

| 검증 항목 | 결과 |
|---|---|
| 문항 수 | TOTAL = 111 (GDPR 69 / CCPA 42) |
| 중복 ID / broken visible_if / status / value | 전부 0건 |
| 하드코딩 gate ID | HARDCODED_MISSING = [] |
| 진단 로직 일관성 | issues = 0 |
| 단일/복수 평가, CCPA 게이트, visible_if 타입 | 전부 정상 |
| Flask 스모크 | HTTP 200/302 OK |
| py_compile | 통과 |
| 랜딩 페이지 렌더링 | 좌표 정렬 확인 + 콘솔 오류 0건 |