# PPAP-Web 웹서버 로컬 실행 방법

작성일: 2026-09-22 | 적용 환경: Windows 10/11 + PowerShell

---

## 1. 사전 준비 (최초 1회)

### 1-1. Python 설치 확인

PowerShell에서 실행:

```powershell
python --version
```

- 권장: Python 3.11 이상 (현재 검증 환경: Python 3.11.9)
- 미설치 시: https://www.python.org/downloads/ 에서 설치 후 **"Add python.exe to PATH"에 체크**

### 1-2. 프로젝트 폴더로 이동

```powershell
cd C:\Users\s2205\OneDrive\Desktop\PPAP-Web
```

### 1-3. 필수 패키지 설치

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

`requirements.txt` 내용 (Flask 3.0.3):

```
Flask==3.0.3
```

> 참고: CSRF 입력(`{{ csrf_input() }}`)을 위해 함께 사용되는 `flask-wtf` 등 추가 패키지가 필요하다면
> `pip install flask-wtf` 로 설치하세요. (현재 requirements에는 Flask만 명시)

---

## 2. 서버 실행 (매회)

### 2-1. 기본 실행

`app.py` 에는 `app.run()` / `__main__` 블록이 **없습니다.** 반드시 `flask run` 명령으로 실행합니다.

**PowerShell에서:**

```powershell
cd C:\Users\s2205\OneDrive\Desktop\PPAP-Web

# 1) 세션 비밀키 환경변수 설정 (아무 문자열이나 가능)
$env:FLASK_SECRET_KEY = '나만의-비밀키-문자열'

# 2) 실행 (한글 로그 보존용 인코딩 설정 포함)
$env:PYTHONIOENCODING = 'utf-8'
python -m flask run --host 127.0.0.1 --port 5000 --no-reload
```

실행 성공 시 콘솔에:

```
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

### 2-2. 브라우저 접속

- 주소: **http://127.0.0.1:5000/**
- 첫 페이지: 랜딩 (세계 지도 + 진단할 법령 선택 + 공지사항·법률개정 소식)

### 2-3. 서버 종료

- 실행한 PowerShell 창에서 `Ctrl + C`

---

## 3. 실행 옵션 설명

| 옵션 | 의미 |
|---|---|
| `--host 127.0.0.1` | 본인 PC에서만 접속 (로컬 전용, 권장) |
| `--host 0.0.0.0` | 같은 네트워크의 다른 기기에서도 접속 가능 |
| `--port 5000` | 포트 번호 (다른 포트 사용 시 `--port 5001` 등) |
| `--no-reload` | 파일 변경 시 자동 재시작 끔 (안정 실행) |
| `--reload` (기본) | 코드/템플릿 변경 시 자동 재시작 (개발 중 유용) |

> **주의**: `--no-reload`로 실행하면 템플릿(`templates/`)·CSS 변경이 **캐시되어 반영되지 않을 수 있습니다.**
> 수정 사항이 보이지 않으면 서버를 재시작하세요 (`Ctrl+C` 후 다시 실행).
> 개발 중에는 `--reload` (또는 옵션 생략)을 권장합니다.

---

## 4. 문제 해결

| 증상 | 해결 |
|---|---|
| `'flask'은(는) 내부 또는 외부 명령...` | pip 설치 확인: `python -m pip install -r requirements.txt` |
| `Address already in use` (포트 충돌) | 기존 서버 종료 후 재실행, 또는 `--port 5001` 사용 |
| 한글이 깨져 보이는 로그 | `$env:PYTHONIOENCODING = 'utf-8'` 설정 후 재실행 |
| 페이지가 예전 화면 (CSS/템플릿 변경 미반영) | `--no-reload` 서버 재시작 (Ctrl+C → 다시 실행) |
| `FLASK_SECRET_KEY` 관련 에러 | 실행 전 `$env:FLASK_SECRET_KEY = '임의의-문자열'` 설정 누락 확인 |
| DB 관련 오류 (첫 실행) | `app.py`의 `with app.app_context(): init_db()` 가 최초 실행 시 자동 초기화 |

---

## 5. 백그라운드 실행 (별도 창 없이)

PowerShell(Start-Process)로 백그라운드 실행:

```powershell
cd C:\Users\s2205\OneDrive\Desktop\PPAP-Web
$env:FLASK_SECRET_KEY = '나만의-비밀키-문자열'
$env:PYTHONIOENCODING = 'utf-8'
$p = Start-Process -FilePath python -ArgumentList '-m','flask','run','--host','127.0.0.1','--port','5000','--no-reload' -WorkingDirectory (Get-Location) -RedirectStandardOutput 'flask_out.log' -RedirectStandardError 'flask_err.log' -PassThru
Write-Output ("서버 PID: " + $p.Id)
```

백그라운드 서버 종료:

```powershell
Stop-Process -Name python -Force
```

> 종료 명령은 python 프로세스 전체를 종료하므로, 다른 python 작업이 있다면 PID로 지정하세요.

---

## 6. 최종 실행 검증 (2026-09-22)

- `python -m flask run --host 127.0.0.1 --port 5000 --no-reload` 실행 후
- `http://127.0.0.1:5000/` → **HTTP 200 확인**
- `/start`(POST), `/diagnosis`, `/result`, 법률개정 패널 JSON 라우트 스모크 테스트 통과

---

## 7. Git / GitHub 배포 주의

- 로컬 폴더는 현재 **git 저장소가 아닙니다** (`git status` → not a git repository)
- GitHub 반영 시 다음 항목은 **업로드 금지**:
  - `instance/ppap.sqlite3` (런타임 DB, 최초 실행 시 자동 생성)
  - `__pycache__/` 폴더
- `.gitignore` (신규 작성 시):

```
instance/
__pycache__/
*.log
flask_out.log
flask_err.log
.venv/
venv/
```