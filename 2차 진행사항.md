# PPAP-Web 2차·3차 통합 진행사항 (2026-09-22 ~ 2026-09-23)

작성일: 2026-09-23 | 통합 범위: `진행사항_정리_2026-09-22.md`(2차) + `3차 진행사항.md`(3차) | 기준 문서: `진행상황.md` (2026-09-13, 1차)

---

## 1. 질문지 PDF 완전 동일화 (data/questions.py) [2차]

- **대상**: `data/questions.py` — 질문지 전면 재구성
- **결과**: 총 **111문항** (GDPR 69 / CCPA 42) — PDF 최종본과 **ID·문항 순서·가시성(visible_if) 조건까지 완전 동일**
- 현재 코드에 존재하던 **4-3/4-4, track_a_opt_out_contents 문항은 제거**
- 진단 로직(`diagnostic_content.py`)은 재검증 완료

### GDPR 작업 상세 (13건)

| # | 문항/영역 | 작업 내용 |
|---|---|---|
| 1 | `gdpr_scope_detail_pseudonym` | visible_if 조건 추가 — '가명화 적용 범위' 후속 문항 가시성 제어 |
| 2 | 섹션1 개인정보 범위 루프 + `gdpr_anonymisation_vs_pseudonymisation` | 신규 문항 추가(5개 루프): 익명화 vs 가명화 식별 여부, judge_pii_scope 루프 가시성 부여 |
| 3 | `gdpr_scope_detail_identifiers` | 목록에서 'ci' 선택지 제거 — CI는 별도 GIS(민감정보) 처리로 이관 |
| 4 | `gdpr_data_minimisation` | 섹션2로 이동 + 타이틀 '9.' 접두 제거 (문항 번호 체계 통일) |
| 5 | `gdpr_sensitive_explicit_consent` | 문항 리네임 + 조건 갱신 (명시적 동의 평가 로직 보강) |
| 6 | `gdpr_third_party_exists` / `transfer_processor_contract` | 'unsure' 선택지 추가, transfer_processor_contract를 role(3-3) 뒤로 삽입, joint/purpose/notification 타이틀 재정렬 |
| 7 | `gdpr_external_transfer_status` | 'unsure' + 'insufficient' 응답 가시성/평가 보강 |
| 8 | `gdpr_external_transfer_special_categories` 외 4건 | special_categories · basic_lawful_basis · mechanism · notice · documentation 의 visible_if를 transfer_ready / transfer_missing 기준으로 재정렬 |
| 9 | `gdpr_ci_eu_exclusion` / `gdpr_ci_security` | EU 이용자 CI·DI 불필요 수집 방지(4-11) / CI 역외전송 보호조치(4-12) — 전면 대체 |
| 10 | `gdpr_admt_special_categories` | 민감정보 기반 자동화된 결정 조건(visible_if) 추가 |
| 11 | `gdpr_cookie_consent_quality` | 'reject_proof' 체크리스트 항목 제거 — 요건 4종(no_precheck/active/purpose_split/info)으로 정리 |
| 12 | Art.28 주석 | 제3자 처리자 계약 관련 주석 갱신 |
| 13 | `gdpr_scope_detail_anonymous` | **visible_if 버그 수정**: excludes(..., 'yes') → includes(..., 'na') (행 441, 507, 523 — 7건). 'yes' 선택지가 없어 항상 참이던 문제 해결, 익명 처리 시 후속 문항 숨김 규약과 통일 |

### CCPA 섹션 0 재배열 (게이트 구조)

진입 게이트(0-1) → 데이터 매핑(0-2) → 옵트아웃 시스템(0-3) 순서로 재배열하고, 미성년자 권리·인센티브 프로그램 등 후속 문항들의 게이트 조건을 전부 yes로 고정.

| 순서 | 문항 ID | 제목 | gate / visible_if |
|---|---|---|---|
| 0-1 | `ccpa_business_scope_gate` | CCPA 적용 대상 여부 | 게이트(진입 판정) |
| 0-2 | `ccpa_data_mapping` | 데이터 매핑 구축 (리네임) | gate = yes |
| 0-3 | `ccpa_opt_out_system` | 옵트아웃 신청 시스템 | gate = yes |
| 0-4 | `ccpa_nondiscrimination` | 비차별 원칙 | gate = yes |
| 0-5 | `ccpa_incentive_program` | 인센티브 프로그램 | gate = yes |
| — | `ccpa_incentive_program_notice` | 인센티브 통지 요건 | gate = yes |
| 0-6 | `ccpa_internal_process` | 내부 프로세스 | gate = yes |

### CCPA 섹션 1~7 편집 상세

| 섹션 | 주요 문항 | 작업 내용 |
|---|---|---|
| 1. 개인정보 범위 | consumer_scope / scope_* / ci_inventory | consumer_scope gate 조건 추가; identifiers 독립 문항 재작성(11개 선택지, 'ci' 제거); **ci_inventory 신규**(CI 등 대체 식별값 인벤토리); scope_* 전부 gate=yes 정렬 |
| 2. 수집·이용 | notice_at_collection / new_purpose / retention / data_minimisation / request_process / sensitive_use_limit | 루프 문항 gate 조건 추가; sensitive_use_limit 조건(게이트 yes & scope_sensitive 'no' 제외) |
| 3. 제3자 제공 | track_a/b / NEW-CC-3-01 / policy_lists / request_contents / aprime / vendor_lists | track_a·track_b 게이트 분기, 루프 gate 추가; **track_a_opt_out_contents 삭제**; vendor_lists gate + unsure 처리 |
| 4. 국외이전 | vendor_contract / ci_security | 루프 구조 제거 → vendor_contract 단독화, **ci_security 신규**(CI 해외 이전 보호조치); 기존 4-3/4-4 제거 |
| 5. 미성년자 | ccpa_minors | 게이트: data_mapping 포함 시 minors / 미포함 시 none — comment 갱신 |
| 6. 자동화된 결정 | admt_exists / admt_significant_decision / admt_controls | 존재 게이트 → 중대한 결정 → 통제 수단 순서 체인 |
| 7. 쿠키 | cookie_usage / cookie_controls | usage 게이트 + controls 게이트 조건(사용 중일 때만) |

### 삭제 · 신규 · 리네임 요약

| 구분 | 문항 ID | 설명 |
|---|---|---|
| 삭제 | `ccpa_track_a_opt_out_contents` | 요청사항: track_a 루프에서 옵트아웃 콘텐츠 문항 제거 |
| 삭제 | CPCA 4-3 / 4-4 (구버전) | 진단 로직 미사용 — 요청대로 제거 |
| 신규 | `ccpa_ci_inventory` | 1-2. CI 등 대체 식별값 인벤토리 관리 (yes/no/na) |
| 신규 | `ccpa_ci_security` | 4-3. CI 해외 이전 보호조치 |
| 리네임 | `ccpa_sale_share_mapping` → `ccpa_data_mapping` | CPRA 개정 용어(data) 반영 — ID·평가 로직·comment 갱신 |
| 리네임 | `gdpr_sensitive_explicit_consent` | 명시적 동의 문항 리네임 + 조건 보강 |

---

## 2. 검증 결과 (모두 통과) [2차]

| 검증 항목 | 결과 |
|---|---|
| 전체 문항 수 | TOTAL = 111 (GDPR 69 / CCPA 42) |
| 중복 문항 ID | DUP_IDS = [] |
| 깨진 visible_if 참조 | BROKEN_VISIBLE_IF = [] |
| 유효하지 않은 status | BROKEN_STATUS = [] |
| 빈 value / 잘못된 choice | BROKEN_VALUE_REFS = [] |
| 하드코딩 gate ID | HARDCODED_MISSING = [] |
| 진단 로직 일관성 | CONSISTENCY issues = 0 |
| 단일/복수 선택 평가 | EVAL_SINGLE_BAD / EVAL_MULTI_BAD = [] |
| CCPA 게이트 규약 | CCPA_GATE_ISSUES = [] |
| visible_if 타입 규약 | VISIBLE_IF_TYPE_BAD = [] |
| Flask 스모크 테스트 | HTTP_SMOKE_OK (/, /start, /diagnosis, /result, 법률개정 패널 JSON 모두 200/302) |
| 파일 컴파일 | py_compile 통과 |

---

## 3. 법률개정 대응 패널 (신규 기능) [2차 → 3차 연장]

- `data/law_updates.py`: 법률 개정 대응 패널 데이터 구성, 외부 소스 조회 시각 표시, 조회 불가 시 등록 정보 기준 폴백
- `app.py`: 법률개정 패널 JSON 제공 라우팅
- `site_content.py`: 법률 패널 문구
- `templates/` + `static/`: 법률개정 소식 카드 UI (모달 상세 포함)
- 3차에서 자동 수집 전면 개편 — 상세는 **8장** 참조

---

## 4. 랜딩 페이지 레이아웃 개편 [2차]

- **1열(왼쪽)**: 세계 지도 카드(상단) + "진단할 법령을 선택하세요" 카드(하단) — 세로 배치
- **2열(오른쪽)**: "공지사항 · 법률개정 소식" 카드 — 1열과 같은 행 시작, 위 정렬
- 지도 카드 최대 높이 460px로 빈 공간 축소
- 텍스트 변경: "법률개정 대응" → **"공지사항" / "법률개정 소식"**
- 반응형(≤1080px): 2열 → 1열로 접힘 (지도 → 선택 → 법률 순서)
- 검증: DOM 좌표 확인, 콘솔 오류 0건, HTTP 200

---

## 5. 세션 작업 정리 PDF 재작성 [2차]

- `PPAP_세션작업정리_2026-09-22.pdf` (4페이지, 132,490 bytes)
- 요약이 아닌 **전체 작업 내용** 포함 (GDPR 13건, CCPA 섹션0~7, 삭제/신규/리네임, 검증 결과표, 파일 목록, 완료 확인)
- 표 셀을 Paragraph 기반으로 수정해 한글 줄바꿈 오버플로우(글자 잘림) 해결
- 검증: pypdf 한글 추출 정상, 결측 글리프 없음, MalgunGothic 임베드

---

## 6. 지도 적용 권역 점 위치·색상 수정 (templates/index.html + static/css/styles.css) [3차]

- 참고 사진(이미지) 기반 위치 지정은 요청 당시 이미지 입력 미지원으로 불가 → `world-map-realistic.svg`(950×520) 실측 좌표로 대체
- **색상은 디자인 시스템 블록색과 통일** (임의 색상 변경 아님 — 기존 GDPR/CCPA 블록 카드 색상 유지)
  - 대한민국 점: 투명 (`fill: transparent`) — 출발 지점 표시만 유지
  - GDPR(EU): GDPR 블록색 파랑 `var(--brand)` #0b5ed7, 활성 상태 #2fa7ff
  - CCPA(미국): CCPA 블록색 청록 `var(--teal)` #147d64, 활성 상태 #37c59c
  - pulse(출발): `rgba(16,42,67,0.12)`
- **위치 실측 좌표** (국가 도형 centroid 기준):

| 요소 | x | y |
|---|---|---|
| 대한민국 dot / pulse / 출발 | 790 | 206 |
| EU·EEA dot (halo) | 468 | 182 (halo rx 64 ry 42) |
| 미국·캘리포니아 dot | 140 | 206 |
| 미국 halo | 206 | 208 (rx 98 ry 54) |
| GDPR 경로 | `M790 206 C720 178,580 156,468 182` | |
| CCPA 경로 | `M790 206 C640 135,340 135,140 206` | |
| 라벨 대한민국 / EU·EEA / 캘리포니아·미국 | (809,200) / (417,169) / (68,195) | |

- 검증: 게재 전 좌표는 사용자 확인 대기 (사진과 대조 필요)

---

## 7. 법률개정 소식 자동 수집 (data/law_updates.py — 핵심 산출물) [3차]

기존에는 법령 개정 데이터가 **정적 레지스트리(`STATIC_LAW_UPDATES`) + 소스 접근 여부(HTTP 핑) 확인**만 하고 있었음. → **실제 데이터를 공식 소스에서 수집**하도록 전면 개편.

### GDPR — EUR-Lex SPARQL 라이브 수집 (`_fetch_gdpr_live`)

- 엔드포인트: `https://publications.europa.eu/webapi/rdf/sparql` (GET, `application/sparql-results+json`)
- GDPR cellar URI: `http://publications.europa.eu/resource/cellar/3e485e15-11bd-11e6-ba9a-01aa75ed71a1`
- 조회 관계 (UNION 쿼리):
  - `cdm:resource_legal_corrects_resource_legal` → 정정(corrigendum) 3건
    - 32016R0679R(01) 2016-11-22 / R(02) 2018-05-23 / R(03) 2021-03-04
  - `cdm:resource_legal_proposes_to_amend_resource_legal` → 개정 제안 2건
    - 52025PC0501 2025-05-21 (SME 확대 간소화 개정안)
    - 52025PC0837 2025-11-19 (Digital Omnibus)
- 제목은 `cdm:work_title`(영문 원제) 실측, 날짜 `cdm:work_date_document` / `resource_legal_date_document`
- EUR-Lex 문서 링크는 CELEX 기반으로 자동 생성

### CCPA — CPPA 규정 페이지 HTML 파싱 (`_fetch_ccpa_live`)

- 소스: `https://cppa.ca.gov/regulations/ccpa_updates.html` (HTTP 200, 파싱 가능)
- "Month D, YYYY – 제목" 패턴 정규식 추출 (로케일 독립 월 매핑, 중복 제거)
- 실측 문서 이벤트 4건:
  - 2024-11-22 Public Notice of Rulemaking
  - 2025-01-13 Extension of Comment Period
  - 2025-05-09 Modifications to Proposed Regulations
  - 2025-09-22 Final Rulemaking Documents
- 정적 항목(2026-01-01 시행, 2027 ADMT, 2028 위험평가·감사)은 병합 유지

### 폴백·캐시 구조

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 데이터 출처 | 정적 레지스트리 (고정) | 공식 소스 실시간 수집 |
| `source_status` | 소스 접근 여부(HTTP ping) | **실제 수집 성공 여부** (`ok`/`fallback`) |
| `fetched_at` | 접근 성공 시 | 수집 성공 시 (전체 실패 시 None) |
| 캐시 TTL | 6시간 | **1시간** |
| 실패 시 | 정적 그대로 | 해당 법령만 정적 폴백 (`errors` 기록) |

### 검증 (모두 통과)

| 검증 항목 | 결과 |
|---|---|
| `get_law_updates(force_refresh=True)` 라이브 수집 | GDPR 8건(정정3+제안2+정적3)/ CCPA 7건(라이브4+정적3), `errors=[]`, status `ok` |
| 수집 실패 시뮬레이션 | 양 법령 `fallback`, 정적 데이터 유지, `fetched_at=None` |
| Flask 라우트 + 템플릿 렌더링 | `GET /` 200, 법률개정 섹션·`fetched_at`·Digital Omnibus·Final Rulemaking Documents 모두 렌더링 |
| 문법 | `ast.parse` / `py_compile` 통과 |
| 응답 계약 | `{fetched_at, errors, laws[].key/title/subtitle/source_status/updates[]}` 유지 — `app.js` 모달·`index.html` 템플릿과 호환 |

---

## 8. 법률개정 소식 카드 레이아웃 개편 (templates/index.html + static/css/styles.css) [3차]

- **배치 구조 변경**: 단일 카드 안에 GDPR/CCPA 블록 세로 배치 → **카드 분리 + grid-area 정렬**
  - 1행: 지도 카드(좌) | **GDPR 법률개정 카드**(우)
  - 2행: 진단 법령 선택 카드(좌) | **CCPA 법률개정 카드**(우)
  - `grid-template-areas: "map gdpr" "sel ccpa"`
- **카드별 독립 스크롤**: 각 카드의 업데이트 목록이 상하 스크롤(최대 높이 344px, `overflow-y: auto`, `overflow-x: hidden`)
  - 제목 길이 오버플로 방지: `overflow-wrap: anywhere` + 항목 `min-width: 0` + 리스트 `grid-template-columns: minmax(0,1fr)`
  - 좌우 스크롤 발생 소지 제거 (상하 스크롤만 동작)
- **최신 항목이 위로**: 모든 updates를 날짜 내림차순 정렬 (SPARQL·CCPA 파싱·정적 폴백 모두)
  - `data-law-index`(`loop.index0`) ↔ JSON 배열이 동일 순서 유지 → 모달 상세 매핑 정상
- **반영 시각 표시**: 각 카드 하단 작은 글씨(0.78rem)로 "GDPR/CCPA 소식 반영 시각: {fetched_at}" 표시, 실패 시 등록 정보 기준 문구
- 미사용 `.law-panel-note`(전역) 제거 → `.law-card-foot`(카드별)로 교체
- 반응형(≤1080px): 1열로 접힘 — 지도 → 선택 → GDPR → CCPA 순서

---

## 9. 서버·검증 상태 [3차]

- Flask 서버: `python -m flask run --host=127.0.0.1 --port=5000` (PID 갱신하며 실행 중 — 템플릿 변경 시 재시작)
- 템플릿·CSS 변경 후 브라우저 검증: HTTP 200, 콘솔 오류 0건, requests failed 0건, 스크롤 방향(상하만)·grid-area 배치·카드 수·스크롤 오버플로 수치 확인

---

## 10. 이번 기간(2차~3차) 수정·생성 파일

| 파일 | 역할 |
|---|---|
| `data/questions.py` | 질문지 전면 재구성 (111문항) — **2차 핵심 산출물** |
| `data/law_updates.py` | 법률개정 자동 수집 전면 개편 (SPARQL 페처 + HTML 파서 + 폴백 + TTL 1h + 내림차순 정렬) — **3차 핵심 산출물** |
| `diagnostic_content.py` | 진단 로직 (find_consistency_issues 포함) — 일관성 검증 통과 |
| `app.py` | Flask 라우팅 — 법률개정 패널 JSON 제공 |
| `site_content.py` | 사이트 콘텐츠 (법률 패널 문구) |
| `templates/index.html` | 랜딩 레이아웃(지도+선택 1열/법률 소식 2열) → 지도 좌표·색상, 법률 카드 2개 분리(grid-area), 카드별 반영 시각 푸터 |
| `templates/diagnosis.html`, `result.html`, `print_report.html`, `base.html` | 프론트엔드 템플릿 |
| `static/css/styles.css` | 스타일 (레이아웃·법률 패널·지도 점 색상·카드 스크롤) |
| `static/js/app.js` | 기능 (법률 패널·가이드 모달) |
| `PPAP_세션작업정리_2026-09-22.pdf` | 세션 작업 전체 정리 PDF (2차) |
| `instance/ppap.sqlite3` | 스모크 테스트로 생성된 런타임 DB (GitHub 미반영) |
| `2차 진행사항.md` | 본 문서 (2차·3차 통합) |

---

## 11. 다음 마일스톤

- **사용자 확인 대기**: 지도 점 좌표(대한민국 790,206 / EU 468,182 / 미국 140,206) 참고 사진과 대조 / 법률개정 최신순·카드 분리·반영 시각 표시 최종 확인
- 개정법률 안내: 국가법령정보센터 조문별 연혁·대비표 / API 존재 여부 확인 후 대응 (진행 중)
- AI 자동진단 방향성 검토 (방향성 문서 수준 정리)
- 논의 후 진행 항목: 로그인/회원가입, 문의 기능, 과징금 유추, 육각형 그래프, Q8·Q9·Q11·Q12·Q15 중복 해소, 가이드 통일, 시행령 리서치