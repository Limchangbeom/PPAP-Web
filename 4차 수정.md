# PPAP-Web 4차 수정 내용 총람 (2026-09-26)

작성일: 2026-09-26 | 반영 대상: `PPAP_진단문항_첨삭.xlsx`의 **'수정 시 체크' 20건** (GDPR 14 / CCPA 6) + 체크 미표시 **1건 별도 대조**(GDPR 4-07, 변경 없음 — §6-3)

---

## 1. 변경 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `data/questions.py` | 4차 반영 문항 정의·문구·번호·평가 규약, `exclusive_values`/`one_of_groups` 파라미터 추가, 제목 번호 33건 통일 |
| `data/guidance.py` | GDPR 제21조 반대권 고지 의무 가이드 추가 (정당한 이익 문항에 연결) |
| `diagnostic_content.py` | `_sanitize_multi_answer()`에 배타 선택 정규화 추가 (서버측 강제) |
| `app.py` | 문항 메타데이터에 `exclusive_values` / `one_of_groups` 전달 |
| `templates/diagnosis.html` | 체크박스에 `data-exclusive-option` / `data-exclusive-group` 속성 추가 |
| `static/js/app.js` | `handleNoneCheckbox` → `handleExclusiveCheckbox` 일반화, `one_of_groups` 클라이언트 강제 |
| `최종_진단문항.xlsx` | **신규 생성** — 112문항 최종본 (GDPR 69 / CCPA 43) |
| `4차 수정.md` | **신규 작성** — 본 문서 |

> 백업: 변경 전 `data/questions.py` 원본을 `data/questions.py.bak4`에 보관함.

---

## 2. 문항 수 변화

| | 3차 | 4차 | 증감 |
|---|---|---|---|
| GDPR | 69 | 69 | — |
| CCPA | 42 | 43 | +1 (`ccpa_cross_border_gpc`) |
| **합계** | **111** | **112** | **+1** |

추가된 1건은 첨삭표에 이미 존재(`CA-4-03`, 체크됨)했으나 코드에 누락되어 있던 문항을 보완한 것이다. 새로 발명한 문항이 아니다.

---

## 3. GDPR 4차 반영 14건

| 영역 번호 | 출처 | 내부 ID | 변경 내용 |
|---|---|---|---|
| GD-1-12 | R13 | `gdpr_indirect_identification` | 사실 확인형 → **정책확인형**으로 변경. "결합하면 식별되는가"는 결합 불가능한 정직한 답을 위반으로 처리하므로, GD-1-06/07과 동일하게 "결합 가능성을 고려해 개인정보로 취급하고 있는가"로 변경 |
| GD-2-01 | R17 | `gdpr_notice_purpose_basis` | 근거를 **제13조 제1항 (c)(d) → 제5조 제1항 (a) 투명성 원칙**으로 변경. (d)는 정당한 이익을 근거로 할 때의 조건부 고지사항이라 다른 적법근거 사업자에게 적용되지 않음 |
| GD-2-02 | R18 | `gdpr_data_minimisation` | 누락되어 있던 제목 번호 **2-02** 부여 |
| GD-2-04 | R20 | `gdpr_consent_requirements` | 반대 항목 2쌍(`purpose_split`/`purpose_split_na`, `clear_request`/`clear_request_na`)을 `one_of_groups`로 상호배제 강제 |
| GD-2-05 | R21 | `gdpr_legitimate_interest_requirements` | **`objection_notice`(반대권 별도 고지) 선택지 추가 및 필수 요건화**. 근거에 `GDPR 제21조 제1항·제4항, 전문 47` 추가 |
| GD-3-04 | R26 | `gdpr_transfer_processor_contract` | 질문·응답 문구를 첨삭표 최종 문구로 일치시킴. 응답 8종을 첨삭표 표기로 교체(지시 준수·비밀유기·처리범위·보안조치·재위탁·권리 지원·반환/삭제·감사 권한), 8개 전부 `required` 유지 |
| GD-2-02 | R39 | `gdpr_transfer_notice_requirements` | **4장(GD-4-08) → 2장 2-02로 이동**, 상시 표시로 변경. 제13조 제1항 필수 고지 항목 6종은 `required`, 국외이전 한정 항목 2종은 `recommended`로 분리(§6-1) |
| GD-5-02 | R44 | `gdpr_child_consent_basis` | "법적 근거가 동의인가?" → **"법적 근거에 동의가 포함되어 있는가?"** |
| GD-5-05 | R47 | `gdpr_child_verification_process` | 질문을 첨삭표 문구로 일치시킴 — "연령과 친권 책임자 확인 절차가 다음 요건을 충족하는가?" → **"다음 중 연령과 친권 책임자 확인 절차에서 충족하는 요건을 모두 고르시오."** 응답·요건 5종은 무변경 |
| GD-5-08 | R50 | `gdpr_child_special_categories` | 단일 판정형 → **목록 선택형** 문장으로 변경 |
| GD-5-09 | R51 | `gdpr_child_privacy_notice` | 단일 판정형 → **목록 선택형** 문장으로 변경 |
| GD-6-06 | R57 | `gdpr_admt_human_review` | **변경 없음** — 이미 일치. 검증만 수행 |
| GD-7-01 | R61 | `gdpr_cookie_usage` | 7장 번호만 실제 코드 순서에 맞춰 재부여(7-01~7-10). 내용 무변경 |
| GD-7-10 | R70 | `gdpr_cookie_policy_accuracy` | "일치 여부"만 묻던 문장을 **"기재되어 있고 실제 작동과 일치하는가"**로 변경. 선택지 4/5개가 "기재되어 있다" 형식이어서 기존 문장과 불일치했음 |

---

## 4. CCPA 4차 반영 6건

| 영역 번호 | 출처 | 내부 ID | 변경 내용 |
|---|---|---|---|
| CA-0-03 | R4 | `ccpa_opt_out_system` | "데이터 유형(**쿠키, 국외이전 등**)" 명시. 응답 4종·판정 로직 무변경 |
| CA-1-02 | R10 | `ccpa_scope_identifiers` | "수집·생성·보유하는가?" → **"…보유하고 개인정보로 취급하는가?"** (GDPR 1장과 동일한 정책확인형 기준) |
| CA-1-08 | R16 | `ccpa_scope_sensitive` | "수집하는가?" → **"수집하고 개인정보로 취급하는가?"** |
| CA-3-01 | R29 | `ccpa_track_a_detection` | `exclusive_values=["unsure"]` 도입. `unsure`와 `none` 동시 선택으로 트랙 A 후속 3개 문항이 숨겨지던 문제 해결. `unsure` 단독 선택 시에만 재확인 문항(3-2) 진행 |
| CA-3-02 | R30 | `ccpa_track_a_unsure_recheck` | 제목에 노출되던 내부 ID **`NEW-CC-3-01` 제거** → "3-2. 판매·공유 가능성 재확인". 판정은 [미흡] 유지 |
| CA-4-03 | R38 | `ccpa_cross_border_gpc` | **신규 문항 추가.** GPC 등 옵트아웃 메커니즘이 국외 이전 건에도 동일 적용되는지 확인 |

`ccpa_cross_border_gpc` 표시조건: `ccpa_business_scope_gate = yes AND ccpa_consumer_scope 미포함 no`
응답: `적용 / 미적용 / 확인 필요 / 해당없음(국내 대상만) / 국외이전 대상 아님`

---

## 5. 4차 번호 통일 33건 (체크 20건 외 누락 결함 수정)

20건 반영 후 **전수 대조 과정에서 별도 결함**을 발견해 함께 수정했다.

### 5-1. 무엇이 문제였나

첨삭표 `안내` 시트의 설명:

> *"영역 번호 — 필터용으로 추가한 컬럼. 원문 영역 표기가 문항마다 달라(예: '2. 수집·이용' / '2. 개인정보 수집·이용 시 동의 필요 여부') 번호 기준으로 통일했다. 원문 표기는 옆 컬럼에 그대로 두었다."*

즉 **`영역 번호`는 원문 PDF 표기가 아니라, 문항마다 제각각이던 번호를 하나로 통일한 기준**이다. 3차에서 2·4·5·7장은 이 통일 체계(`2-01`~)로 바꿨지만, **1·3·6장은 원문 PDF 표기가 그대로 남아 있었고 실제 문항 수와 어긋나고 있었다.**

| 장 | 변경 전(원문 PDF 표기) | 문제 | 변경 후 |
|---|---|---|---|
| 1장 | `1.` `2-1.`~`2-8.` `3.`~`8.` 혼용 | 두 체계 혼용 | `1-01`~`1-15` (15문항) |
| 3장 | `3-0.` ~ `3-8.` | **0부터 시작**, 9개인데 0~8 | `3-01`~`3-09` (9문항) |
| 6장 | `6-2.` ~ `6-10.` | **2부터 시작**, 9개인데 2~10 | `6-01`~`6-09` (9문항) |

### 5-2. 변경 범위

**제목 문자열 33건만 변경**했다. 백업(`questions.py.bak4`)과 라인 단위 대조 결과:

- 변경 라인 수 **33건**, 그중 title이 아닌 라인 **0건**
- `question` 본문 · 응답 선택지 · 평가 규약 · 표시 조건 **무변경**
- 1장은 title과 question이 동일 문자열인 구조라, title 문자열 전체가 일치하는 라인만 치환해 question 오염을 방지함

**추가 재번호 (R39 장 이동에 따른 2·4장 밀림)**: `gdpr_transfer_notice_requirements`를 2장으로 옮기면서 2장은 6→7문항, 4장은 11→10문항이 되었다. 이에 따라 2장 5건과 4장 3건의 제목 접두번호를 추가로 재부여했다. 2장 `2-03`~`2-07`, 4장 `4-08`~`4-10`이 이에 해당한다. 이 재번호 역시 **제목 문자열만** 변경했으며, question 본문·응답·평가 규약·표시 조건은 건드리지 않았다. (작업 전 백업: `questions.py.bak5`)

### 5-3. 장별 최종 번호

| GDPR | 1장 1-01~1-15 (15) | 2장 2-01~2-07 (7) | 3장 3-01~3-09 (9) | 4장 4-01~4-10 (10) | 5장 5-01~5-09 (9) | 6장 6-01~6-09 (9) | 7장 7-01~7-10 (10) |
|---|---|---|---|---|---|---|---|
| **CCPA** | 0장 0-01~0-07 (7) | 1장 1-01~1-10 (10) | 2장 2-01~2-10 (10) | 4장 4-01~4-03 (3) | 5장 5-01 (1) | 6장 6-01~6-03 (3) | 7장 7-01~7-02 (2) |

CCPA 3장(`Q-A` / `3-02` / `Q-B` / `트랙 A`·`A'`·`B`)은 첨삭표가 이대로 표기한 항목이라 일괄 번호를 부여하지 않고 원표기를 유지했다.

---

## 6. 판단 기록 (구조 변경과 판정 근거)

### 6-1. R39 국외이전 고지 문항을 2장으로 이동 (실행)

첨삭표 R39 비고 지적을 **그대로 실행했다.** — *"이 부분이 국외이전시에만 보이게 되어있어서 gd-2-01을 이 문항처럼 체크리스트로 바꾸거나, 이 문항을 GD-2-01밑에 넣거나 하는 게 나을 듯 합니다."*

**판단 근거**: GDPR 제13조 제1항의 고지 의무(처리 목적·근거, 개인정보 항목, 수령자, 보유기간, 정보주체 권리, 감독기관에 대한 민원 제기권)는 **국외이전 여부와 무관하게 모든 컨트롤러에게** 적용된다. 그런데 이 문항이 4장(국외이전)에 있고 표시조건도 `gdpr_external_transfer_status ∈ {transfer_ready, transfer_missing}`로 묶여 있어, 국외이전이 없는 사업자에서는 이 법적 의무를 확인할 수 없는 구조였다.

**실행 내용**

| 항목 | 이전 | 이후 |
|---|---|---|
| 위치 | 4장 `4-08` (GD-4-08) | 2장 `gdpr_notice_purpose_basis` 바로 아래 `2-02` (GD-2-02) |
| 표시 조건 | 국외이전 상태 종속 | **상시 표시** (`visible_if` 제거) |
| 평가 규약 | 5종 전부 `required` (국외이전 한정) | 공통 6종 `required` / 국외이전 한정 2종 `recommended` |
| 근거 조문 | 제13조(1)(f), 제14조(1)(f), 제15조(2) | 제13조(1), 제14조(1), 제15조(2) |
| 번호 | 2장 2-01~2-06 / 4장 4-01~4-11 | 2장 2-01~**2-07** / 4장 4-01~**4-10** |

**`required` / `recommended` 분리 이유**: 국외이전 관련 2종(`transfer_recipient`, `transfer_safeguards`)은 이전이 실제 발생할 때만 성립한다. 이들을 `required`로 두면 국외이전이 없는 사업자가 문항을 상시 노출받으면서도 항상 위반 판정을 받게 되므로, 공통 고지 의무와 조건부 고지 의무를 평가 규약 차이로 구분했다.

**첨삭표 표기와의 차이**: 첨삭표 `영역 번호`는 GD-4-08으로 남아 있으나, 동일 행의 비고가 2장 이동을 명시적으로 지시하고 GDPR 제13조 제1항의 적용 대상이 국외이전 사업자 외 전체 컨트롤러이므로 코드에서는 2장 배치를 채택했다. 이에 따라 2장 7문항 / 4장 10문항으로 재정렬했다.

### 6-2. `objection_notice`를 필수 요건으로 판정

제6조 제1항 (f) 자체에는 고지 의무가 없다. 그러나 이 근거를 쓰면 제21조 제1항의 반대권이 자동 발생하고, 제21조 제4항이 **"늦어도 최초 의사소통 시점까지, 다른 정보와 명확히 구분하여"** 명시 고지를 요구한다. 이익형량 요건(`stated`/`balanced`)과 무관한 **독립적 법정 의무**이므로 별도 필수 항목으로 두었고, 미선택 시 기존 required 부분선택 규칙에 따라 **[미흡]**으로 판정된다.

### 6-3. 체크 미표시 행 GD-4-07 별도 대조 (변경 없음)

`gdpr_transfer_derogation_requirements`(4-07. 제49조 예외 사용 요건)은 첨삭표 38행에 있으나 **'수정 시 체크'가 비어 있어** 4차 20건 대상에 포함되지 않았다. 4차 종료 시 해당 행을 별도로 대조했다.

**대조 결과 — 전 항목 일치, 코드 변경 없음**

| 항목 | 첨삭표 | 코드 | 판정 |
|---|---|---|---|
| 질문 | 제49조 예외를 사용한다면 일시적·예외적 상황이며 해당 예외의 엄격한 요건을 입증할 수 있는가? | 동일 | 일치 |
| 표시 조건 | `gdpr_transfer_mechanism = derogation` | 동일 | 일치 |
| 응답 | 10항 | 10항 (순서·문구까지 동일) | 전 항목 일치 |
| 질문 변경 사항 | (비어 있음) | — | 변경 지시 없음 |
| 응답 변경 사항 및 근거 | (비어 있음) | — | 변경 지시 없음 |
| 비고 | (비어 있음) | — | 변경 지시 없음 |

평가 규약도 첨삭표의 응답 구성이 **"제49조 예외사유 7개 중 최소 1개 + 공통 확인사항 3개 전부"** 구조를 요구하는 것과 부합한다(`at_least_one_values` 7항, `required_values` 3항).

즉 이 행은 멘토가 지적할 사항이 없어 체크를 비워 둔 것이며, **"미반영"이 아니라 "반영 불필요"** 다. 다만 산출물에서 표기되지 않아 누락으로 오인될 수 있으므로, `최종_진단문항.xlsx`에 **"확인"(파란색)** 마커로 기록하고 본 절에 남긴다.

> **참고 — R39 이동으로 생긴 부수 효과**: 첨삭표가 이 행의 `응답 후 연결`에 제시한 다음 문항은 `gdpr_transfer_notice_requirements`인데, 4차 R39에서 해당 문항을 2장으로 이동했다(§6-1). 따라서 4장 흐름은 derogation → `gdpr_transfer_documentation`으로 이어지며, 첨삭표의 연결 문구는 현재 코드 배치를 반영하지 않는다. 코드에는 이 "다음 문항" 연결을 구현한 로직이 없어(문항별 `visible_if` + 페이지 분할 방식) 기능상 영향은 없다.

---

## 7. 배타 선택 규약 구현

`exclusive_values` / `one_of_groups`는 **서버와 브라우저 양쪽에서** 강제한다. 한쪽만 하면 조작으로 우회되거나, 반대로 서버만 하면 화면과 실제 판정이 어긋난다.

| 위치 | 구현 |
|---|---|
| `data/questions.py` | `multi_presence_question()` / `checklist_question()`에 파라미터 추가, `evaluation`에 반영 |
| `diagnostic_content.py` | `_sanitize_multi_answer()`가 선언 순서상 **첫 번째 선택만 유지** |
| `app.py` | 문항 복사본에 두 속성 전달 |
| `templates/diagnosis.html` | `data-exclusive-option` / `data-exclusive-group` 속성 출력 |
| `static/js/app.js` | `handleExclusiveCheckbox()` — 선택 시 같은 그룹 나머지 해제, 미선택 시 복원 |

적용 문항:

- `ccpa_track_a_detection` — `exclusive_values=["unsure"]` (4차 신규)
- `gdpr_consent_requirements` — `one_of_groups` 2쌍 (4차 신규)
- `gdpr_lawful_bases` — `one_of_groups` 1그룹(6개 적법근거) · **기존 `[수정-PDF]` 항목으로 4차 범위 밖**

`none` 처리는 기존 `_sanitize_multi_answer()` 방식을 그대로 유지했다(별도 규약).

---

## 8. 검증 결과

| 항목 | 결과 |
|---|---|
| 총 문항 수 | **112** (GDPR 69 / CCPA 43) |
| 중복 내부 ID | **0건** |
| 중복 영역 번호 | **0건** |
| title 번호 vs 영역 번호 | 전수 일치 (미번호 표기 `Q-A`·`트랙 A` 제외) |
| 장별 번호 연속성 | 전 장 1..N 연속, 결번·중복 없음 |
| 백업 대비 변경 | 33라인, 전부 title |
| question 본문 오염 | 없음 |
| `POST /start` | 302 → `/diagnosis?page=1` |
| `GET /diagnosis` | 200, 페이지 렌더 정상 |
| `data-exclusive-option="true"` | 정상 출력 (`unsure`만 해당) |
| `data-exclusive-group` | 정상 출력 (동의 문항 2쌍 = group 0/1) |
| `NEW-CC-3-01` 노출 | **0건** (제목에서 제거 확인) |
| `unsure`+`retargeting` 동시 선택 | 서버 정규화 후 `["unsure"]` |
| `none`+`retargeting` 동시 선택 | 서버 정규화 후 `["retargeting"]` (`none` 규약 유지) |
| `objection_notice` 미선택 | `insufficient` 판정 |
| GDPR 제21조 가이드 | 정당한 이익 문항에 2건 모두 노출 |

---

## 9. 산출물

| 파일 | 내용 |
|---|---|
| `최종_진단문항.xlsx` | 시트 `GDPR`(69) / `CCPA`(43) / `안내`. 16개 컬럼(영역 번호, 원문 영역 표기, 내부 ID, 최종 제목·질문, 판단 포인트, 근거, 표시 조건, 응답, 평가 규약, 4차 변경·내용·출처). 4차 변경 20행은 노란색, 신규 1행은 초록색 음영 |
| `4차 수정.md` | 본 문서 |

---

## 10. 참고 사항

- `data/questions.py` 4~8행의 `[수정-PDF]` 태그 관례에 따라 4차 변경 지점에는 `[수정-4차-*]` 태그를 남겼다.
- 3장·6장 번호 재정렬은 화면 표시 순서를 바꾸지 않는다(코드의 `QUESTIONS` 순서는 그대로이고 접두번호 문자열만 바뀜).
- `ccpa_track_a_unsure_recheck`는 코드 내부 ID가 `ccpa_track_a_unsure_recheck`이고, 첨삭표 내부 ID 컬럼에는 `NEW-CC-3-01`로 남아 있다. 사용자에게 노출되는 **제목**에서 `NEW-CC-3-01`을 제거한 것이 R30의诉求다.
